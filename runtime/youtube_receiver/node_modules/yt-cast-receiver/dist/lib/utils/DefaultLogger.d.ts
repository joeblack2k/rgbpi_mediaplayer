import { type LogLevel } from './Logger.js';
import type Logger from './Logger.js';
export default class DefaultLogger implements Logger {
    protected level: LogLevel;
    protected color: boolean;
    constructor(color?: boolean);
    error(...msg: any[]): void;
    warn(...msg: any[]): void;
    info(...msg: any[]): void;
    debug(...msg: any[]): void;
    setLevel(value: LogLevel): void;
    protected checkLevel(targetLevel: LogLevel): boolean;
    protected process(targetLevel: LogLevel, msg: any[]): void;
    protected toStrings(msg: any[]): string[];
    protected toOutput(targetLevel: LogLevel, msg: string[]): void;
}
//# sourceMappingURL=DefaultLogger.d.ts.map