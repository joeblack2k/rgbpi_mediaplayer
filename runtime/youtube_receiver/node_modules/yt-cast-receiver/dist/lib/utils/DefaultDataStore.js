var __classPrivateFieldSet = (this && this.__classPrivateFieldSet) || function (receiver, state, value, kind, f) {
    if (kind === "m") throw new TypeError("Private method is not writable");
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a setter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot write private member to an object whose class did not declare it");
    return (kind === "a" ? f.call(receiver, value) : f ? f.value = value : state.set(receiver, value)), value;
};
var __classPrivateFieldGet = (this && this.__classPrivateFieldGet) || function (receiver, state, kind, f) {
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a getter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
    return kind === "m" ? f : kind === "a" ? f.call(receiver) : f ? f.value : state.get(receiver);
};
var _DefaultDataStore_storage;
import storage from 'node-persist';
import DataStore from './DataStore.js';
/**
 * Default `DataStore` implementation that uses [node-persist](https://github.com/simonlast/node-persist) to persist data.
 */
class DefaultDataStore extends DataStore {
    constructor() {
        super();
        _DefaultDataStore_storage.set(this, void 0);
        __classPrivateFieldSet(this, _DefaultDataStore_storage, storage.create(), "f");
        __classPrivateFieldGet(this, _DefaultDataStore_storage, "f").init().catch((error) => this.logger.error('[yt-cast-receiver] DefaultDataStore: caught error in storage.init():', error));
    }
    // eslint-disable-next-line @typescript-eslint/no-unnecessary-type-parameters
    async set(key, value) {
        try {
            await __classPrivateFieldGet(this, _DefaultDataStore_storage, "f").set(key, value);
        }
        catch (error) {
            this.logger.error(`[yt-cast-receiver] Failed to set value for key '${key}' in data store:`, error, 'Value:', value);
        }
    }
    async get(key) {
        try {
            return await __classPrivateFieldGet(this, _DefaultDataStore_storage, "f").get(key) || null;
        }
        catch (error) {
            this.logger.error(`[yt-cast-receiver] Failed to get value for key '${key}' in data store:`, error);
            return null;
        }
    }
    async clear() {
        return __classPrivateFieldGet(this, _DefaultDataStore_storage, "f").clear();
    }
}
_DefaultDataStore_storage = new WeakMap();
export default DefaultDataStore;
//# sourceMappingURL=DefaultDataStore.js.map